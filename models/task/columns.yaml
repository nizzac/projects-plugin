# ===================================
#  List Column Definitions
# ===================================

columns:
    title:
        label: Title

    user[full_name]:
        label: Assignee

    project[name]:
        label: Project

    status:
        label: Status

    due_date:
        label: Due date
        type: datetime
        format: d/m/y

    estimate_string:
        label: Estimated Time